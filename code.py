def create_phone_number(n):
    first = random.randint(0,9) * 3
    secomf = random.randint(0,9) * 3
    last = random.randint(0,9) * 4
    phone_numbe = print({first}-{secomf}-{last})
    return phone_numbe
create_phone_number(phone_numbe)
